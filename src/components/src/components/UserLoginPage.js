import React from 'react';

const UserLoginPage = () => {
    return (
        <div className="user-login-container">
            <h1>User Login</h1>
            <p>Enter your credentials to log in as a User.</p>
        </div>
    );
};

export default UserLoginPage;